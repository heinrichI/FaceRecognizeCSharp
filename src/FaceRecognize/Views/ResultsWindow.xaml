<Window x:Class="FaceRecognize.Views.ResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:converters="clr-namespace:FaceRecognize.Converters"
        mc:Ignorable="d"
        Title="Scan Results" Height="600" Width="900"
        WindowStartupLocation="CenterOwner">
    <Window.Resources>
        <converters:ImagePathToBitmapConverter x:Key="ImgPath" />
    </Window.Resources>
    <DockPanel Margin="16">
        <!-- Summary -->
        <StackPanel DockPanel.Dock="Top" Margin="0,0,0,12">
            <TextBlock FontSize="18" FontWeight="Bold" Text="Scan Results" />
            <TextBlock Foreground="Gray">
                <Run Text="Images scanned: " />
                <Run Text="{Binding TotalImagesScanned, Mode=OneWay}" FontWeight="SemiBold" />
                <Run Text="  |  Faces found: " />
                <Run Text="{Binding TotalFacesFound, Mode=OneWay}" FontWeight="SemiBold" />
            </TextBlock>
        </StackPanel>

        <!-- Known Faces -->
        <GroupBox DockPanel.Dock="Top" Header="Known Faces" Margin="0,0,0,12">
            <ListView ItemsSource="{Binding KnownFaces}" MaxHeight="200">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Thumbnail" Width="80">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <Image Source="{Binding ImagePath, Converter={StaticResource ImgPath}}"
                                           Width="50" Height="50" Stretch="UniformToFill" />
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Name" DisplayMemberBinding="{Binding Name}" Width="150" />
                        <GridViewColumn Header="Confidence" Width="100">
                            <GridViewColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock>
                                        <Run Text="{Binding Confidence, StringFormat={}{0:P1}}" />
                                    </TextBlock>
                                </DataTemplate>
                            </GridViewColumn.CellTemplate>
                        </GridViewColumn>
                        <GridViewColumn Header="Image" DisplayMemberBinding="{Binding ImagePath}" Width="300" />
                    </GridView>
                </ListView.View>
            </ListView>
        </GroupBox>

        <!-- Unknown Clusters -->
        <GroupBox Header="Unknown Face Clusters">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <ItemsControl ItemsSource="{Binding UnknownClusters}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,0,12" Padding="8" BorderBrush="LightGray"
                                    BorderThickness="1" CornerRadius="4">
                                <StackPanel>
                                    <TextBlock FontWeight="SemiBold">
                                        <Run Text="Cluster #" />
                                        <Run Text="{Binding ClusterId, Mode=OneWay}" />
                                        <Run Text=" — " />
                                        <Run Text="{Binding FaceCount, Mode=OneWay}" />
                                        <Run Text=" faces" />
                                    </TextBlock>
                                    <ItemsControl ItemsSource="{Binding Faces}" Margin="8,4,0,0">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <WrapPanel />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Image Source="{Binding ImagePath, Converter={StaticResource ImgPath}}"
                                                       Width="60" Height="60" Stretch="UniformToFill"
                                                       Margin="0,0,4,4" />
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </GroupBox>
    </DockPanel>
</Window>
